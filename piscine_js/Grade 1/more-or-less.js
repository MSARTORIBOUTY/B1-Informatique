/*chaque ligne déclare une fonction*/ 

const more = (n) => n + 1; /*prend un seul argument et l'additionne*/
const less = (m) => m - 1;
const add = (o, v) => o + v;/*Prend deux arguments*/ 
const sub = (i, j) => i - j;